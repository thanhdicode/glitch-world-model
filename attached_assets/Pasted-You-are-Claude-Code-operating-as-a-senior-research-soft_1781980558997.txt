You are Claude Code operating as a senior research software engineer, ML systems auditor, Kaggle/CI harness engineer, and scientific reproducibility reviewer.

Repository:
`thanhdicode/glitch-world-model`

Project:
Latent World Models for Video Game Glitch Detection: leakage-aware evaluation of JEPA/LeWM-style latent surprise for glitch detection.

Current pain:
The project keeps failing late on Kaggle after long runs. Each fix passes normal unit tests, but then a new integration bug appears during R5-WOB staged evaluation. This wastes hours.

Known recent failure chain:

1. Kaggle auto-extracted seed artifact tarballs into folders, requiring repack.
2. Full LeWM runtime install pulled environment extras and failed on `box2d-py`.
3. Lean runtime solved imports.
4. Staged R5-WOB runner imported `_lance_dataset` from `scripts/run_gate7_lance_scoring.py`, but the real helper was in `src/glitch_detection/lewm_lance_eval.py`.
5. After that fix, `materialize_lance` created `.lance` outputs as directories, but marker code treated them like regular files and called `sha256_file()`, causing `IsADirectoryError`.
6. The deeper issue appears to be not one isolated bug, but missing integration contracts and missing Kaggle-like local harness tests.

Your task:
Do not just patch the next error. Read the whole project, reconstruct why this failure pattern keeps happening, identify the deepest engineering/process root cause, and implement a robust harness strategy so future Kaggle full runs are not used as the first real integration test.

STRICT SAFETY BOUNDARIES:

* Do not touch locked test.
* Do not materialize locked rows.
* Do not score locked rows.
* Do not fabricate WOB metrics.
* Do not add performance claims.
* Do not run R5-XGAME unless R5-WOB output is validated.
* Do not run R6 unless its dependencies are satisfied.
* Do not commit raw data, Kaggle outputs, Lance datasets, checkpoints, tarballs, logs, `.env`, `kaggle.json`, credentials, or local cache.
* Do not replace scientific protocol to make the code pass faster.
* Any smoke/synthetic result must be clearly marked as non-paper-valid.

PHASE 1 — FULL CONTEXT READ

Read these first:

* `RULES.md`
* `AGENTS.md`
* `PLAYBOOK.md`
* `README.md`
* `docs/context/BOOT.md`
* `docs/context/PROJECT_STATE.md`
* `docs/context/NEXT_ACTION.md`
* `docs/context/LAST_HANDOFF.md`
* `docs/context/TASK_ROUTER.md`
* `docs/research/16_claim_registry.md`
* `docs/research/82_r5_wob_nonlocked_evaluation_pipeline.md`
* `docs/research/84_r5_wob_result_intake_template.md`
* `docs/research/85_r6_ablation_plan.md`
* `docs/research/86_kaggle_r5_wob_success_failure_playbook.md`
* `docs/research/87_r5_wob_failure_audit_and_retry_plan.md`
* all R5-WOB scripts and tests
* all LeWM Lance/eval modules
* all Kaggle runner scripts
* paper table placeholders

Then inspect:

* `src/glitch_detection/r5_wob_staged.py`
* `src/glitch_detection/r5_wob_eval.py`
* `src/glitch_detection/lewm_data.py`
* `src/glitch_detection/lewm_adapter.py`
* `src/glitch_detection/lewm_lance_eval.py`
* `scripts/run_r5_wob_stage.py`
* `scripts/run_r5_wob_identical_episode_evaluation.py`
* `scripts/validate_r5_wob_evaluation.py`
* `scripts/validate_wob_seed_artifacts.py`
* `scripts/verify_r5_wob_upload.py` if present
* `scripts/run_r5_xgame_comparison.py`
* `cloud/wob_r5_eval/run_kaggle_r5_wob_staged.sh`
* all tests touching R5-WOB, Lance, upload verification, XGAME, R6, paper tables, claim registry

PHASE 2 — FAILURE CHRONOLOGY AND ROOT-CAUSE AUDIT

Create a root-cause audit answering:

1. What exactly failed each time?
   Classify every known failure:

* dependency failure;
* artifact-layout failure;
* wrong module boundary;
* file/directory type contract failure;
* missing local harness;
* incomplete regression testing;
* Kaggle/local parity failure;
* stage observability failure;
* protocol-risk issue.

2. Why did normal tests pass but Kaggle failed?
   Investigate:

* Were tests too shallow?
* Did tests mock only the happy path?
* Did tests not simulate Kaggle’s auto-extracted dataset layout?
* Did tests not simulate `.lance` as directory?
* Did tests not enforce CLI-vs-library boundaries?
* Did tests not check stage outputs as real filesystem objects?
* Did tests not verify complete runner command from shell to Python and back?
* Did tests not exercise the actual Kaggle runner script?
* Did tests not define a contract for artifacts, Lance outputs, stage markers, and receipts?

3. What is the deepest root cause?
   Do not stop at “one function was wrong.”
   Find the system-level cause. Examples to evaluate:

* no explicit interface contracts between stages;
* CLI scripts being reused as libraries;
* weak artifact type model: file vs directory vs tarball vs dataset;
* no Kaggle-like synthetic integration harness;
* no fail-fast gate before full Kaggle runs;
* no mandatory staged smoke run;
* no contract tests for stage marker schemas;
* no local reproduction of failure_debug bundles;
* over-reliance on “full Kaggle evaluation” as integration test;
* weak output receipts and provenance gates;
* insufficient separation between paper-valid evaluation and engineering smoke tests.

PHASE 3 — ARCHITECTURAL CONTRACTS

Define explicit contracts for R5-WOB staged evaluation.

For each stage:

* `preflight`
* `materialize_lance`
* `baseline_scores`
* `lewm_seed42`
* `lewm_seed43`
* `lewm_seed44`
* `aggregate_metrics`
* `validate_package`

Specify:

* inputs;
* outputs;
* expected filesystem object types;
* required metadata;
* allowed side effects;
* forbidden side effects;
* stage marker JSON schema;
* failure-debug schema;
* resume/skip rule;
* validation rule;
* paper-valid vs smoke-only status;
* locked-test safety fields.

Important object-type contracts:

* tarball = file;
* `.sha256` sidecar = file;
* `.lance` output may be a directory;
* extracted seed artifact root = directory;
* checkpoint weights = file;
* stage marker = JSON file;
* score CSV = file;
* metrics JSON = file;
* output bundle = tar.gz file;
* failure debug bundle = tar.gz file.

PHASE 4 — HARNESS ENGINEERING

Implement a robust local fail-fast harness so Kaggle is no longer the first integration test.

Create or improve:

1. `scripts/local_r5_wob_failfast_gate.py`

It must run quickly without full WOB data and check:

* all R5-WOB staged modules import correctly;
* stage CLI arguments parse correctly;
* all stage names are valid;
* `glitch_detection.lewm_lance_eval` exposes required helpers:

  * `_lance_dataset`
  * `METADATA_KEYS`
  * `canonical_rows_from_samples`
  * `validate_manifest_rows`
* R5-WOB staged code does not call `_lance_dataset` from CLI wrapper scripts;
* `_file_record()` supports both file and directory;
* `.lance` directory hashing/inventory works;
* stage marker JSON is schema-valid;
* failure-debug parser returns structured fields;
* success intake refuses missing or invalid SHA256;
* XGAME refuses to run without a validated WOB receipt;
* smoke outputs cannot be mistaken for paper-valid outputs.

2. Synthetic Kaggle-like integration harness

Create tests that simulate:

* `/kaggle/input` layout;
* Kaggle auto-extracted seed artifact folders;
* sidecar `.sha256` files;
* fake WOB normal/test roots;
* fake seed artifact metadata;
* fake `.lance` directories;
* stage marker files;
* failure debug bundles;
* success output bundles;
* missing/corrupt sidecars;
* wrong object types.

This harness should catch the recent bugs:

* using CLI wrapper instead of `lewm_lance_eval`;
* treating `.lance` directory as file;
* accepting incomplete failure bundles;
* accepting output bundles without valid receipts;
* allowing XGAME before validated WOB.

3. Runner-level shell test

Add a lightweight test or script that validates:

* `cloud/wob_r5_eval/run_kaggle_r5_wob_staged.sh` resolves environment variables correctly;
* stage order is correct;
* output paths do not collide;
* failure trap packages useful debug files;
* full runner can be dry-run in synthetic mode if supported.

4. Optional staged smoke mode

Ensure there is a `R5_WOB_SMOKE=1` or equivalent mode that:

* uses tiny synthetic or tiny real subset;
* runs end-to-end through all stages;
* marks output as smoke-only;
* refuses claim/paper-valid ingestion;
* produces a clear result quickly.

PHASE 5 — PROCESS FIX

Update docs so the project no longer repeats the same mistake.

Add a rule:
No full Kaggle R5-WOB retry may be given to the human until these pass:

* `python scripts/local_r5_wob_failfast_gate.py`
* focused R5-WOB staged tests
* regression test for the exact previous failure
* `python -m pytest -q`
* `python -m ruff check .`
* `python -m ruff format --check .`
* `python scripts/check_claim_registry.py`
* `python scripts/validate_context_cache.py`
* `python scripts/doctor.py`
* `python scripts/validate_research_release.py --ci`
* `pre-commit run --all-files`
* `git diff --check`
* forbidden artifact scan

If any of these fail, do not tell the human to run Kaggle.

PHASE 6 — EXPLAIN WHY THE PROJECT KEPT FAILING

Write a clear explanation for the human in Vietnamese or English:

Explain:

* why the project kept failing despite “tests passing”;
* why local unit tests were not enough;
* why Kaggle exposed errors late;
* why the true issue is missing harness/contracts, not just bad coding;
* how the new fail-fast gate prevents repeat failures;
* what still cannot be validated locally and must remain Kaggle-only;
* what exact command must be run before future Kaggle retries.

Do not blame the human. This is an engineering process issue.

PHASE 7 — IMPLEMENTATION REQUIREMENTS

Implement the minimal safe changes needed to make the harness real.

Expected files may include:

* `scripts/local_r5_wob_failfast_gate.py`
* `tests/test_r5_wob_failfast_gate.py`
* `tests/test_r5_wob_contracts.py`
* `tests/test_r5_wob_kaggle_synthetic_harness.py`
* `docs/research/89_r5_wob_root_cause_and_harness_audit.md`
* updates to `docs/context/NEXT_ACTION.md`
* updates to `docs/context/LAST_HANDOFF.md`
* updates to `docs/research/86_kaggle_r5_wob_success_failure_playbook.md`
* updates to `docs/research/87_r5_wob_failure_audit_and_retry_plan.md`

Keep the changes focused. Do not rewrite the whole project.

PHASE 8 — VALIDATION

Run:

* `python scripts/local_r5_wob_failfast_gate.py`
* focused new tests
* `python -m pytest -q`
* `python -m ruff check .`
* `python -m ruff format --check .`
* `python scripts/check_claim_registry.py`
* `python scripts/validate_context_cache.py`
* `python scripts/doctor.py`
* `python scripts/validate_research_release.py --ci`
* `pre-commit run --all-files`
* `git diff --check`
* forbidden artifact scan

Also check:

* no Kaggle outputs committed;
* no `.lance` directories committed;
* no checkpoints committed;
* no tarballs committed;
* no credentials committed;
* locked test remains closed.

PHASE 9 — FINAL REPORT FORMAT

Return exactly this structure:

# R5-WOB Root-Cause and Harness Engineering Report

## 1. Executive Result

State one:

* HARNESS_READY
* ROOT_CAUSE_FOUND_BUT_FIX_INCOMPLETE
* BLOCKED_NEEDS_HUMAN_INPUT
* BLOCKED_CRITICAL

## 2. Deepest Root Cause

Explain the deepest cause in 3–8 bullets.

## 3. Why The Same Failure Pattern Kept Happening

Explain why code passed tests but failed on Kaggle.

## 4. Failure Chain Reconstruction

List each known failure, its class, and whether the new harness catches it.

## 5. Contract Gaps Found

List missing or weak contracts:

* stage contract;
* filesystem object type;
* artifact layout;
* CLI/library boundary;
* validator/receipt;
* smoke vs paper-valid.

## 6. Harness Implemented

List scripts/tests/docs added or updated.

## 7. Commands That Must Pass Before Any Future Kaggle Retry

List exact commands.

## 8. What Can Be Tested Locally vs What Still Requires Kaggle

Separate clearly.

## 9. Human Explanation

Explain in simple terms why the project kept failing and how the fix prevents it.

## 10. Safe Next Kaggle Action

Give the next Kaggle command/cell only if all local gates passed.
If not passed, do not provide Kaggle full-run instructions.

## 11. Tests / Checks

List PASS/FAIL for every command.

## 12. Commit / Branch

Give branch name and commit SHA.

## 13. One Human Next Action

Give exactly one next action.
