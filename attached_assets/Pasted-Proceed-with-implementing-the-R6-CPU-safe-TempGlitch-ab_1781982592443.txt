Proceed with implementing the R6 CPU-safe TempGlitch ablation runner A1–A4.

Repository: `thanhdicode/glitch-world-model`

Current project status:

* R5 TempGlitch is completed and validated.
* R5-WOB is still awaiting Kaggle output.
* R5-XGAME must remain blocked until validated R5-WOB output exists.
* R6 WOB must remain blocked until validated R5-WOB output exists.
* Locked test remains closed and must not be touched.

Goal:
Implement the local CPU-safe R6 TempGlitch ablation infrastructure only. Do not run WOB ablations. Do not fabricate metrics. Do not add performance claims.

Implement A1–A4:

A1 — Aggregation comparison

* Compare existing aggregation modes from validated R5 TempGlitch score files:

  * mean
  * max
  * top-k / top2 if available
  * episode_mean if available
* Produce ablation table with AUROC, AUPRC, F1, and bootstrap CI if score files support it.

A2 — Surprise distance comparison

* Compare available surprise/distance variants:

  * L2
  * cosine if already available or computable from existing score artifacts
* If cosine is not available from validated artifacts, do not invent it. Mark as `NOT_AVAILABLE_FROM_CURRENT_ARTIFACTS`.

A3 — Threshold calibration sweep

* Evaluate threshold behavior using only non-locked validated R5 TempGlitch scores.
* Include calibration source, threshold rule, F1, precision, recall, FPR, TPR if available.
* Keep leakage-aware protocol.

A4 — Failure-mode analysis

* Summarize which TempGlitch categories/episodes are detected well/poorly based on validated R5 score artifacts.
* If failure-mode labels are missing, create a clear placeholder/report saying what metadata is missing.

Strict boundaries:

* Do not touch locked test.
* Do not materialize locked test.
* Do not score locked test.
* Do not use WOB output unless validated R5-WOB receipt exists.
* Do not run R5-XGAME.
* Do not run R6 WOB.
* Do not fabricate missing metrics.
* Do not add claims such as SOTA, generalization, SIGReg benefit, or action-conditioning benefit.
* Do not commit raw data, outputs, checkpoints, tarballs, `.lance` directories, `.env`, `kaggle.json`, logs, or credentials.

Required files to create/update:

* `scripts/run_r6_tempglitch_cpu_ablations.py`
* `scripts/validate_r6_ablations.py`
* `docs/research/90_r6_tempglitch_cpu_ablation_plan_and_runner.md`
* `paper/tables/r6_ablation_results.tex`
* tests for the runner and validator
* update `docs/context/NEXT_ACTION.md`
* update `docs/context/LAST_HANDOFF.md`
* update claim registry only with narrow infrastructure/preparation claim if appropriate, not performance claims.

Runner requirements:

* Fail closed if required validated R5 TempGlitch score files are missing.
* Print exactly which files are required.
* Accept explicit input paths.
* Write outputs only under a safe output directory.
* Mark outputs as:

  * `paper_valid=true` only if all required validated inputs exist and validator passes;
  * otherwise `paper_valid=false`.
* Include provenance:

  * input file paths
  * input hashes
  * protocol notes
  * locked_test_materialized=false
  * locked_test_scored=false

Validator requirements:

* Verify all expected R6 output tables exist.
* Verify no WOB dependency was used.
* Verify no locked-test field is true.
* Verify no placeholder is treated as a metric.
* Verify generated LaTeX table does not contain fake numbers.

Tests/checks:
Run:

* `python scripts/local_r5_wob_failfast_gate.py` if available
* focused R6 tests
* `python -m pytest -q`
* `python -m ruff check .`
* `python -m ruff format --check .`
* `python scripts/check_claim_registry.py`
* `python scripts/validate_context_cache.py`
* `python scripts/doctor.py`
* `python scripts/validate_research_release.py --ci`
* `pre-commit run --all-files`
* `git diff --check`
* forbidden artifact scan

Commit safe code/docs only.

Commit message:
`r6: add tempglitch cpu-safe ablation runner`

Final report format:

# R6 TempGlitch CPU-Safe Ablation Runner Report

## 1. Executive Result

State one:

* R6_TEMPGLITCH_CPU_READY
* BLOCKED_MISSING_R5_SCORE_FILES
* BLOCKED_NEEDS_FIX

## 2. What Was Implemented

List scripts/docs/tests/tables.

## 3. A1–A4 Coverage

Explain what each ablation does and whether it is runnable now.

## 4. Required Input Files

List exact R5 TempGlitch score/provenance files required.

## 5. Claim Safety

Confirm no WOB, no locked test, no fabricated metrics, no performance overclaim.

## 6. How To Run

Give exact command.

## 7. How To Validate

Give exact validation command.

## 8. Tests / Checks

List PASS/FAIL.

## 9. Commit

Give branch and commit SHA.

## 10. One Human Next Action

Give exactly one next action.
