# MASTER PROMPT FOR CLAUDE OPUS / CLAUDE CODE — FIX R5 WOB LANCE WINDOW MANIFEST FAILURE + UPGRADE LeWM-Glitch REPO TO RESEARCH-GRADE

You are Claude Code operating as an elite senior ML research engineer, reproducibility auditor, memory/resource-debugging specialist, and paper-readiness engineer.

You are working inside the repository:

`thanhdicode/glitch-world-model`

Project theme:

**LeWM-Glitch / Latent Surprise: Temporal Video Game Glitch Localization via Lightweight JEPA / Latent Dynamics**

Current goal:

1. Diagnose and fix the current Kaggle R5 WOB failure that keeps getting stuck at `materialize_lance: window manifest start`.
2. Audit the current repo for hidden instability, leakage, memory spikes, broken scripts, reproducibility gaps, and paper-readiness issues.
3. Upgrade the repo toward the strongest academic direction: lightweight latent/world-model-based temporal game glitch detection, with honest benchmark protocol, no overclaim, no fake results, and professor/reviewer-grade reproducibility.

---

## 0. Non-negotiable rules

Follow these strictly:

1. Do not claim benchmark results that were not generated.
2. Do not touch locked test data unless a command/stage explicitly and safely allows it.
3. Preserve the locked-test guards:

   * `locked_test_materialized=false`
   * `locked_test_scored=false`
4. Preserve protocol safety:

   * validation buggy data must not be used for fitting/threshold selection.
   * calibration/threshold selection must use normal/validation data only.
   * test/locked data must remain held out.
5. Do not change labels, split meaning, metric definitions, or manifest contract unless you first justify it and update tests/docs.
6. Do not “fix” by increasing Kaggle RAM, batch size hacks, or skipping manifest validation. Fix the underlying resource behavior.
7. Every patch must include tests or at least a runnable smoke check.
8. Every output claim must be evidence-tagged:

   * verified by code/log
   * inferred hypothesis
   * unverified future work
9. Prefer small, auditable patches over large rewrites.
10. If something cannot be proven from the repo/log, say so explicitly.

---

## 1. Current failure summary — self-contained evidence

I cannot upload files to you, so here is the key evidence.

The failed run completed all Lance writing successfully:

```text
Train Lance: 48/48 episodes written
Validation normal Lance: 12/12 episodes written
Validation buggy Lance: 60/60 episodes written
```

The process died immediately after:

```text
[r5_wob] materialize_lance: window manifest start normal=/kaggle/working/glitch-world-model/outputs/r5_wob_identical_episode/_wob_validation_normal.lance buggy=/kaggle/working/glitch-world-model/outputs/r5_wob_identical_episode/_wob_validation_buggy.lance output=/kaggle/working/glitch-world-model/outputs/r5_wob_identical_episode/_window_manifest.csv
```

Failure metadata:

```json
{
  "failure_class": "possible_resource_exhaustion",
  "phase": "materialize_lance",
  "exit_code": 120,
  "traceback_present": false,
  "git_sha": "e0f6e2b51c40d10d6018daa66f3b6a570d4311f4"
}
```

Important hashes:

```text
r5_wob_staged log SHA256:
42c95733998e7698b86192a3b3a2b2709612106c28cab950a2dd43d899967127

failure debug tarball SHA256:
806d96f2c74f0ff50140faff8ab500270d34e31d0538f8fe4e26392f1e35e638
```

Two logs named `(2)` and `(3)` were identical byte-for-byte, so this is not two independent failures. It is a reproducible failure at the same phase.

The wrapper classified the failure as `possible_resource_exhaustion`, but we cannot claim 100% OOM because Kaggle did not provide peak RAM/cgroup memory-event proof in the bundle. Treat the leading hypothesis as:

> high-probability resource exhaustion / native Lance runtime hard kill during window manifest construction, with possible native error that does not surface as Python traceback.

---

## 2. Suspect code area

Locate current code by function names, because line numbers may shift.

Primary suspect:

`src/glitch_detection/r5_wob_staged.py`

Function:

```python
def _build_window_manifest(
    *,
    normal_lance: Path,
    buggy_lance: Path,
    eval_rows: list[dict[str, str]],
    output_path: Path,
) -> tuple[list[dict[str, str]], dict[str, str]]:
    normal_dataset = _lance_dataset(normal_lance, include_metadata=True)
    buggy_dataset = _lance_dataset(buggy_lance, include_metadata=True)
    normal_samples = [
        {key: str(normal_dataset[index][key]) for key in METADATA_KEYS}
        for index in range(len(normal_dataset))
    ]
    buggy_samples = [
        {key: str(buggy_dataset[index][key]) for key in METADATA_KEYS}
        for index in range(len(buggy_dataset))
    ]
    ...
    manifest_rows = [
        *canonical_rows_from_samples(... normal_samples ...),
        *canonical_rows_from_samples(... buggy_samples ...),
    ]
    validate_manifest_rows(...)
    write_csv_rows(output_path, manifest_rows, MANIFEST_FIELDS)
    return manifest_rows, fingerprints
```

Secondary suspect:

`src/glitch_detection/lewm_lance_eval.py`

Current loader pattern:

```python
METADATA_KEYS = (
    "source",
    "source_episode_id",
    "pair_id",
    "category",
    "label",
    "split",
    "action_mode",
)

def _lance_dataset(path: Path, *, include_metadata: bool) -> Any:
    import stable_worldmodel as swm
    keys = ["pixels", "action"]
    if include_metadata:
        keys.extend(METADATA_KEYS)
    return swm.data.LanceDataset(
        path=path.resolve().as_posix(),
        num_steps=4,
        frameskip=1,
        keys_to_load=keys,
    )
```

Why this is dangerous:

1. `_build_window_manifest()` opens two Lance datasets at the same time: normal and buggy.
2. Even when only metadata is needed, `_lance_dataset(... include_metadata=True)` still loads `pixels` and `action`.
3. The dict comprehension calls `dataset[index]` once per metadata key, so a single window may be loaded up to 7 times:

   ```python
   {key: str(normal_dataset[index][key]) for key in METADATA_KEYS}
   ```
4. The code holds all of these simultaneously:

   * `normal_dataset`
   * `buggy_dataset`
   * `normal_samples`
   * `buggy_samples`
   * `manifest_rows`
5. The previous streaming writer fixed the Lance writing phase, but this is a second memory peak during reading Lance back to build `_window_manifest.csv`.

This explanation matches the observed log: all Lance writes complete, then the process dies exactly when manifest construction starts.

---

## 3. Immediate mission

First, inspect the current repo. Do not assume the code exactly matches the pasted snippets.

Run:

```bash
git rev-parse HEAD
git status --short
rg -n "_build_window_manifest|build_canonical_manifest|_metadata_samples|_lance_dataset|LanceDataset|METADATA_KEYS|write_csv_rows|validate_manifest_rows" src scripts tests cloud docs
```

Then open and audit at least:

```text
src/glitch_detection/r5_wob_staged.py
src/glitch_detection/lewm_lance_eval.py
src/glitch_detection/kaggle_automation.py
src/glitch_detection/wob_lance_materialization.py or equivalent Lance writer module
scripts/run_r5_wob_stage.py
cloud/wob_r5_eval/run_kaggle_r5_wob_staged.sh
tests/ related to r5_wob, lewm_lance_eval, manifest, locked-test guards
```

Your first deliverable should be a concise diagnosis:

```text
A. Confirmed code path at death
B. Why it can hard-kill without traceback
C. Which part is proven from logs
D. Which part is still hypothesis
E. Exact patch plan
```

---

## 4. Required fix direction

Implement a resource-safe manifest construction path.

### 4.1 Do not use pixel/action loader for metadata

Add a true metadata-only reader.

Preferred design:

```python
def iter_lance_metadata_samples(path: Path) -> Iterator[dict[str, str]]:
    ...
```

This function must read only:

```python
METADATA_KEYS = (
    "source",
    "source_episode_id",
    "pair_id",
    "category",
    "label",
    "split",
    "action_mode",
)
```

It must not request/load:

```python
pixels
action
```

Investigate which approach is safest in the repo/runtime:

Option A — if `stable_worldmodel.data.LanceDataset` supports metadata-only keys:

```python
swm.data.LanceDataset(
    path=...,
    num_steps=4,
    frameskip=1,
    keys_to_load=list(METADATA_KEYS),
)
```

Option B — if stable_worldmodel requires `pixels/action`, bypass it for manifest metadata and use the native Lance reader, for example:

```python
import lance
dataset = lance.dataset(path)
scanner = dataset.scanner(columns=list(METADATA_KEYS), batch_size=...)
for batch in scanner.to_batches():
    ...
```

Option C — if native Lance schema stores metadata differently, inspect schema and implement the minimal safe column reader. Document exactly what you found.

Do not guess. Inspect the actual Lance schema or writer code.

### 4.2 Read each window once

Never do this:

```python
{key: str(dataset[index][key]) for key in METADATA_KEYS}
```

This causes repeated `dataset[index]` calls.

Use this pattern if index access is unavoidable:

```python
sample = dataset[index]
metadata = {key: str(sample[key]) for key in METADATA_KEYS}
```

But the preferred fix is streaming batch metadata from Lance without index access.

### 4.3 Process normal and buggy sequentially

Avoid opening normal and buggy datasets together.

Correct pattern:

```python
fingerprints = compute fingerprints first without open datasets

open/read normal metadata -> write/validate normal rows -> close/free/gc

open/read buggy metadata -> write/validate buggy rows -> close/free/gc
```

Explicitly release references:

```python
del dataset
gc.collect()
```

If PyTorch might be loaded:

```python
torch.cuda.empty_cache()
```

only if applicable.

### 4.4 Stream manifest rows to CSV

Avoid holding `normal_samples`, `buggy_samples`, and `manifest_rows` together.

Refactor so `_build_window_manifest()` can return only metadata needed by the stage marker:

```python
return {
  "window_row_count": count,
  "dataset_fingerprints": fingerprints,
  "calibration_episode_count": ...,
  ...
}
```

If the caller currently needs `len(manifest_rows)`, change the caller to use returned count.

Preserve the CSV schema exactly:

```python
MANIFEST_FIELDS = (
    "window_id",
    "dataset_name",
    "dataset_fingerprint",
    "dataset_window_index",
    "source",
    "source_episode_id",
    "pair_id",
    "category",
    "label",
    "split",
    "action_mode",
    "evaluation_role",
)
```

### 4.5 Preserve manifest validation

The existing `validate_manifest_rows()` validates a full in-memory list. Replace or supplement it with streaming validation.

Required invariants:

1. Manifest not empty.
2. `window_id` unique.
3. No row has locked/test split.
4. Label must be `normal` or `buggy`.
5. Buggy rows must never be `calibration_normal`.
6. `evaluation_role` only in:

   * `calibration_normal`
   * `evaluation`
7. Calibration episode count must match expected.
8. Calibration episodes must come from normal rows only.
9. Window IDs must be deterministic:

   * `normal_validation:00000000`, etc.
   * `buggy_probe:00000000`, etc.
10. Row count must equal metadata windows read.

Because row count is not huge in the current run, keeping a `set(window_ids)` is acceptable. What must be avoided is holding image/action payloads and all samples from both datasets simultaneously.

### 4.6 Also fix duplicated manifest path in `lewm_lance_eval.py`

Audit these functions too:

```python
_metadata_samples()
build_canonical_manifest()
run_gate7_scoring()
_score_dataset()
```

If `build_canonical_manifest()` has the same bug, fix it too. It should use the new metadata-only streaming path or a memory-safe list path.

Do not leave two divergent manifest builders where one is fixed and one is still unsafe.

---

## 5. Telemetry and failure localization

Add lightweight telemetry around the phase that failed.

Implement or reuse a helper like:

```python
def resource_snapshot(label: str) -> dict[str, Any]:
    # include:
    # - rss_mb if psutil available
    # - ru_maxrss_mb via resource.getrusage
    # - cgroup memory.current if available
    # - cgroup memory.events if available
    # - timestamp
```

Log snapshots at these points:

```text
before fingerprint
after fingerprint normal
after fingerprint buggy
before opening normal metadata reader
after normal len/schema
every N normal metadata windows
after closing normal
before opening buggy metadata reader
after buggy len/schema
every N buggy metadata windows
after closing buggy
before csv finalization
after csv finalization
```

Also include telemetry in a JSON artifact:

```text
outputs/.../resource_telemetry_materialize_lance.json
```

Do not make telemetry mandatory if `psutil` is unavailable. Use fallback stdlib.

---

## 6. Tests to add or update

Add tests that would have caught this issue without Kaggle.

### 6.1 Unit test: metadata extraction calls dataset[index] only once

If using fake dataset:

```python
class FakeDataset:
    def __getitem__(self, index):
        self.calls[index] += 1
        return {...metadata..., "pixels": HUGE_SENTINEL, "action": HUGE_SENTINEL}
```

Test that metadata extraction does not call `__getitem__` once per key.

### 6.2 Unit test: `_lance_dataset(... include_metadata=True)` is not used for manifest metadata

If the final design bypasses `_lance_dataset`, test the manifest builder uses the metadata-only reader.

### 6.3 Unit test: streaming validation catches locked/test split

Construct rows with split `test` or `locked` and assert failure.

### 6.4 Unit test: buggy rows cannot become calibration

Construct a buggy sample with source episode in calibration set; assert role remains evaluation or validation rejects invalid role.

### 6.5 Regression test: manifest field order unchanged

Assert exact `MANIFEST_FIELDS`.

### 6.6 Smoke test

Run:

```bash
python -m pytest -q
```

If full test suite is too slow, at least run:

```bash
python -m pytest -q tests/test_lewm_lance_eval*.py tests/test_r5_wob*.py
```

Then run a smoke version of the R5 stage if available:

```bash
python scripts/run_r5_wob_stage.py --stage materialize_lance ... --smoke
```

Use the repo’s documented command if the exact CLI differs.

---

## 7. Kaggle rerun plan after patch

After local tests pass, prepare a minimal Kaggle rerun.

Goal:

1. Verify the crash moved past `window manifest`.
2. Confirm `_window_manifest.csv` is written.
3. Confirm `materialize_lance` stage marker is written.
4. Confirm `locked_test_materialized=false`.
5. Confirm memory telemetry exists and shows bounded RSS.
6. Do not run the full expensive pipeline unless the materialize stage passes.

Expected new log should include:

```text
materialize_lance: window manifest start ...
materialize_lance: window manifest normal complete ...
materialize_lance: window manifest buggy complete ...
materialize_lance: window manifest complete rows=<N> output=.../_window_manifest.csv
materialize_lance: writing stage marker
```

If it fails again, collect:

```text
last 100 log lines
failure_summary.json
resource_telemetry_materialize_lance.json
manifest partial path status
directory listing of Lance datasets
cgroup memory events if available
```

---

## 8. Additional repo audit — fix likely future failures while you are here

After the immediate failure is fixed, audit the repo for issues that could block the next stage.

Focus areas:

### 8.1 Resource safety

Search for patterns like:

```python
list(dataset)
[dataset[i] for i in range(len(dataset))]
dataset[index] repeated inside dict/list comprehension
loading pixels/action for metadata-only tasks
opening train/normal/buggy Lance datasets simultaneously
holding all scores/frames/windows in memory unnecessarily
```

Fix high-risk cases if they are in the R5/Gate7 path.

### 8.2 Data leakage

Audit all simple experiment scripts and scorer APIs.

Known risk pattern:

```python
normal_records = [r for r,l in zip(records,labels) if l == 0]
fit_model(normal_records)
evaluate_scores(... full data ...)
```

This can leak test normals into fitting or threshold selection if used on real benchmark data.

Make sure paper-facing scripts use:

```text
train normal only -> fit model
validation -> threshold selection
test -> final report only
```

Add warnings or rename unsafe scripts as demo-only if they are leaky.

### 8.3 Import bugs

Audit scripts that import sibling files directly, e.g.:

```python
from create_dynamics_test_dataset import main
```

If they fail from repo root, fix using proper package import or script path insertion.

### 8.4 Stage markers and cache invalidation

Make sure cached stage markers include enough hashes to invalidate stale outputs when code/protocol changes.

### 8.5 Reproducibility artifacts

Every research run should emit:

```text
git_sha
command
seed
input data fingerprints
split fingerprint
runtime versions
output file hashes
locked_test flags
protocol name
```

### 8.6 Paper-facing outputs

Make sure the repo can eventually produce:

```text
outputs/final_metrics_table.json
paper/tables/main_results.tex or .md
paper/figures/score_timeline.png
paper/figures/architecture.svg or .png
docs/research/claim_registry.md or equivalent
```

Do not fabricate these outputs. Only create scaffolds/templates if no real results exist.

---

## 9. Strategic upgrade according to strongest research direction

The repo should not be a generic “bug detector” project. Upgrade and align it to this precise academic thesis:

> Temporal game glitches are violations of learned gameplay dynamics. A lightweight latent dynamics/world-model approach can detect them through latent prediction error (“surprise”) more robustly than simple pixel-level baselines under an honest leakage-free evaluation protocol.

Use this positioning throughout docs, README, comments, and scripts.

### 9.1 Safe paper scope

Primary safe scope:

```text
Latent surprise / latent dynamics proxy for temporal video game glitch detection.
```

Acceptable method wording:

```text
lightweight latent dynamics proxy
JEPA-inspired latent prediction
PCA + linear transition as proxy
future full LeWorldModel integration
```

Do not write:

```text
We use full LeWorldModel
SOTA
beats VLMs
detects all glitches
creates a new dataset
```

unless the repo actually proves it.

### 9.2 Required research-grade components

The repo should support:

1. Public benchmark or verified fallback benchmark.
2. Fixed train/val/test split by source/video/episode, not random window leakage.
3. Fit only on train normal.
4. Threshold only on validation.
5. Test only once.
6. AUROC, F1, precision, recall.
7. Per-category breakdown if labels permit.
8. Temporal IoU / event metrics if span labels permit.
9. Ablation:

   * latent error vs frame_diff
   * smoothing on/off
   * latent_dim sweep
10. Failure mode analysis:

* camera motion false positives
* subtle physics glitch false negatives
* identical/near-identical episode artifacts
* label ambiguity

11. Reproducible output tables and figures.

### 9.3 Upgrade docs

Update or create docs only with factual status:

```text
docs/research/current_state.md
docs/research/r5_wob_failure_analysis.md
docs/research/r5_wob_manifest_memory_fix.md
docs/research/evaluation_protocol.md
docs/research/claim_registry.md
docs/research/paper_readiness_gap.md
```

Each claim should be tagged:

```text
VERIFIED
INFERRED
PLANNED
BLOCKED
```

### 9.4 Upgrade README

README should clearly separate:

```text
What is implemented
What has been verified
What is currently blocked/fixed
What is future work
How to reproduce a smoke test
How to reproduce a paper run
What not to claim yet
```

---

## 10. Expected final response from you

When you finish, report in this exact structure:

```text
# Claude Code Report — R5 WOB Window Manifest Failure

## 1. Diagnosis
- Confirmed failure point:
- Root cause:
- Evidence:
- Still uncertain:

## 2. Code Changes
List files changed and why.

## 3. Resource Safety Improvements
Explain how metadata-only reading works.
Explain how repeated dataset reads were eliminated.
Explain how normal/buggy are processed sequentially.
Explain how manifest validation remains safe.

## 4. Protocol Safety
Confirm:
- locked_test_materialized:
- locked_test_scored:
- validation_buggy_used_for_fit_select:
- threshold/test leakage status:

## 5. Tests Run
Include exact commands and results.

## 6. Kaggle Rerun Instructions
Exact minimal command to rerun the fixed stage.
Expected success markers.
Artifacts to download.

## 7. Repo Research-Grade Upgrade
List P0/P1/P2 next steps:
P0 = needed to stop current failure
P1 = needed for reliable R5 results
P2 = needed for FISAT/paper quality

## 8. Remaining Risks
Be honest. Do not overclaim.
```

---

## 11. Priority order

Do the work in this order:

1. Confirm current code state.
2. Fix metadata-only manifest construction.
3. Add resource telemetry.
4. Add/adjust tests.
5. Run tests.
6. Audit for duplicated unsafe Lance metadata code.
7. Patch duplicated paths.
8. Update docs with verified failure analysis.
9. Prepare Kaggle rerun command.
10. Then do broader research-grade upgrade plan.

Do not start paper writing before the R5 pipeline gets unstuck.

Start now.
