\section{Artifact Provenance}
\label{app:artifact-provenance}

The current paper-facing results are only as trustworthy as the artifacts behind them. Table
\ref{tab:artifact-hashes} records the key input and output hashes for the qualified R5 family.

\input{tables/artifact_hashes}

These hashes are not ornamental metadata. They define the bounded evidence package for the present
manuscript state.
