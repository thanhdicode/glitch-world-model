\section{Bounded Results}
\label{sec:results}

TODO: keep this section limited to validated, hash-addressable result families. Do not add
ablation, WOB, locked-test, or cross-dataset result prose without a corresponding validated
artifact and claim-registry update.

Table~\ref{tab:r5-bounded-results} summarizes the current qualified comparison surface. The table
caption explicitly states the permitted scope: \emph{Non-locked TempGlitch validation-only
identical-episode family.}

\input{tables/r5_bounded_results}

Within this frozen family, the best observed AUROC and AUPRC rows came from seed44 LeWM variants,
while a \texttt{frame\_diff} configuration produced the strongest bounded F1 row. These are exact
family-qualified observations, not broad method claims. The confidence intervals are wide enough to
reinforce that this paper should emphasize provenance and containment over triumphal framing.

The safe interpretation is therefore modest: the repository now contains a full provenance-bound
R5 evaluation bundle and a reproducible bounded comparison table. The unsafe interpretation would
be to promote these rows into a general claim of superiority, state of the art, or cross-dataset
readiness. Appendix~\ref{app:forbidden-claims} makes that distinction explicit.

\subsection{World of Bugs Results (Pending)}
\label{sec:results-wob}

% PLACEHOLDER — do not insert WOB numbers until the R5-WOB output tarball is
% uploaded, SHA256-verified, extracted, validator-passed, and metrics ingested.
Table~\ref{tab:r5-wob-results} is reserved for the World of Bugs R5 identical-episode evaluation.
At the time of writing, the R5-WOB evaluation is running on Kaggle and results remain unverified.
No WOB detection-performance claim is supported until the output artifact is validated.

\input{tables/r5_wob_results}

\subsection{Cross-Dataset Comparison (Pending)}
\label{sec:results-xgame}

% PLACEHOLDER — do not insert cross-dataset numbers until both R5-TempGlitch
% and R5-WOB outputs are validated and R5-XGAME comparison completes.
Table~\ref{tab:r5-xgame-results} is reserved for the cross-dataset comparison between TempGlitch
and World of Bugs under matched reporting discipline. No cross-game generalization claim is
supported until both datasets have validated evaluation evidence.

\input{tables/r5_xgame_results}

\subsection{Ablation Results (Pending)}
\label{sec:results-ablation}

% PLACEHOLDER — do not insert ablation numbers until R6 ablations complete.
Table~\ref{tab:r6-ablation-results} is reserved for the R6 minimal decision-relevant ablation
study. No SIGReg benefit, action-conditioning benefit, or aggregation-superiority claim is
supported until the corresponding ablation outputs exist and are validated.

\input{tables/r6_ablation_results}
