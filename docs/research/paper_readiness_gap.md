# Paper Readiness Gap

## VERIFIED Ready

- Non-locked staged pipeline, streaming Lance materialization, named baselines, three LeWM seed scoring, SHA256 intake, and provenance-bound R5-WOB output validation.

## BLOCKED Not Ready

- A negative-inclusive binary WOB benchmark.
- Cross-game/source generalization evidence.
- Pre-specified ablations and paper-quality figures/tables based on valid binary metrics.
- A locked-test decision or locked-test result.

## PLANNED

Use R5-XGame only after a frozen, leakage-checked manifest contains held-out normal negatives and buggy positives. Paper-facing claims must remain synchronized with `16_claim_registry.md`.
